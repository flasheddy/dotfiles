[ids]
*

[main]
# Overload capslock: tap = esc, hold = control
# Shift + capslock = caps lock (toggled)
capslock = overload(control, esc)
# If you prefer to have the toggle capability explicitly:
# capslock = overload(control, capslock)
